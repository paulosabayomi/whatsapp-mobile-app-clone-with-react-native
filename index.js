/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import ProviderComp from './components/Providers';

AppRegistry.registerComponent(appName, () => ProviderComp);
